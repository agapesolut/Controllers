# Infraestrutura local

O `docker-compose.yml` sobe um PostgreSQL de apoio ao desenvolvimento do sistema.

## Uso

```bash
cd docker
docker compose up -d
```

## Servicos

- `postgres`: banco do sistema
- `pgadmin`: interface opcional para consulta e administracao
